# Working agreements

- Create and switch to a new branch before making changes in this Git repository.
- Skip branch creation only when the user explicitly asks to work on the current branch.
- Always use the Conventional Commits format for commit messages (for example, `feat: add responsive sources`, `fix: preserve image dimensions`, or `chore: update tooling`).
